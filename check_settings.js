import mongoose from "mongoose"; import dotenv from "dotenv"; dotenv.config(); import { Settings } from "./src/modules/settings/settings.model.js"; async function run() { await mongoose.connect(process.env.DATABASE_URI || process.env.MONGODB_URI); console.log("DB DATA:"); console.log(await Settings.findOne({ isSingleton: "global" }).lean()); process.exit(0); } run().catch(console.error);
